Overview
A PHP-based weather application that fetches and displays current weather details (such as temperature, humidity, pressure, wind speed/direction) from a weather API, displaying 
imagery according to the weather conditions

Procedure:
Make sure the neccessary code is present.
Open Xampp, Start the Apache mmodule
Run the project in the chrome,,  http://localhost/WEBSITE(folder name)/
